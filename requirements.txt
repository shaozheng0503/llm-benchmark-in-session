# Runtime（所有脚本的基础依赖）
# Python 3.9+ 标准库已覆盖大部分需求，下面是真正需要的第三方包

# HTTP 调用 judge/compare/adversarial 等需要
# （无第三方依赖——使用标准库 urllib.request）

# 可视化
matplotlib>=3.8

# 仅下列脚本需要：
#   - radar.py          ：matplotlib
#   - meta_eval.py      ：无（手写 Cohen's Kappa）
#   - calibration.py    ：无（手写 ECE）

# 开发工具（可选）
pytest>=7.0
ruff>=0.1
